; OpenTrust SIP — Asterisk Voicemail Screening
; Place in /etc/asterisk/extensions.conf

[opentrust-voicemail]

; Entry point for voicemail-only calls
exten => s,1,Voicemail(${ARG1}@opentrust)
    same => n,Hangup()

exten => h,1,NoOp(Voicemail screening call ended)
